#!/bin/bash

while true; do
  ./Overpass/translate_xapi "*[seamark:type=*][@meta]" | ./Overpass/osm3s_query > tmp.osm 2> /dev/null;
	mv tmp.osm world.osm
	../util/smfilter -a 0.01 < world.osm | ../util/tagfilter > tmp.osm
	mv tmp.osm next.osm
done
